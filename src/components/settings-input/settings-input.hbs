<div class="settingItem">
    <div class="settingTitle">
        {{ title }}
    </div>
    <div class="settingInput">
        <input type="{{ type }}" title="{{ title }}" name="{{ name }}" value="{{value}}">
    </div>
</div>